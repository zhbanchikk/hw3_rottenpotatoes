require 'spec_helper'
 
describe MoviesController do
  describe 'searching same director movies' do
    
    it 'should call the model method that performs same director search'

    it 'should select the Search Results template for rendering'

    it 'should make the same director search results available to that template'

   end
end